import {ProfileImageContainer, ProfileImage} from '../styles/ProfileStyles';

const Profile = () => {
    return (
      <ProfileImageContainer>
        <ProfileImage src='/img/jean.png' alt="Nome da pessoa" />
      </ProfileImageContainer>
    );
  };
  
  export default Profile;